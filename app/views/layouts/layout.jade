- var body_id = typeof body_id == "undefined" ? '' : body_id
!!! 5
html
  head
    title Compass sitemap designer, by Skookum

    - if (body_id == 'index')
      link(rel='stylesheet', href='/styles/home.css' ) 
    - else
      link(rel='stylesheet', href='/styles/app.css' ) 

  body(id=body_id)
    != body